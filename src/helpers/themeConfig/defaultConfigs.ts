import { theme, ThemeConfig } from "antd";

export const componentsConfig: ThemeConfig["components"] = {
    Input: {
        controlHeight: 45
    },
    Button: {
        controlHeight: 45
    }
}

export const tokenConfig: ThemeConfig["token"] = {
    borderRadius: 10
}